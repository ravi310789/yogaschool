import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "images.unsplash.com",
      },
    ],
  },
  // Temporary placeholder image host for design work. Replace with proper Supabase Storage hosting before launch.
};

export default nextConfig;
